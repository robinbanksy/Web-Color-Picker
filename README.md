# Web-Color-Picker
Simple JS demo of a web color picker

## Features
- RGB sliders to control color display
- Displays both in RGB and Hex-RGB formats (e.g. RGB(R, G, B) and #RRGGBB)
- Generates random colors
- Allows user to save colors to access later

## Future Features
- HSL and HSV support
- Color scheme generator/manager